[project]
name = "marila-demo"
version = "0.1.0"
description = "boto3 demos that round-trip marila's AWS-JSON façades"
requires-python = ">=3.10"
dependencies = [
    "boto3>=1.41",       # ships the s3vectors + s3tables clients
    "botocore>=1.41",
]
