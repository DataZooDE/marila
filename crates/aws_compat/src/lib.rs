//! Cross-service AWS wire-compatibility primitives.
//!
//! Today this is empty scaffolding; the error envelope and SigV4 parser
//! land alongside the first slice that needs them
//! (`CreateVectorBucket`).
